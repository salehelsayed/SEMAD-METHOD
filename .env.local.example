# Local environment overrides for concurrency caps on a Mac M4
# Copy to .env.local and tweak values as needed

# Test runners
JEST_MAX_WORKERS=75%

# Dependency analysis (per-file impact checks)
BMAD_MAX_PARALLEL_IMPACT=8

# QA fixes (independent fixes in parallel)
BMAD_MAX_PARALLEL_FIXES=6

# Documentation workflows
BMAD_MAX_PARALLEL_DOC_FETCH=6
BMAD_MAX_PARALLEL_DOC_PROCESS=8
BMAD_MAX_PARALLEL_DOC_WRITE=8

# Indexing docs (scan/read)
BMAD_MAX_PARALLEL_DOC_INDEX=8

# Notes:
# - CI can set lower values (e.g., 50% for JEST, 3–4 for others)
# - Tools/tasks may detect these variables where supported; otherwise use as guidance
